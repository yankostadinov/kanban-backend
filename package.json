{
  "name": "kanban-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && cd ../kanban/ && yarn prod && cp -r dist ../kanban-backend",
    "deploy": "git push heroku master",
    "deploy:full": "yarn build && git add . && git commit -m build-deploy && yarn deploy"
  },
  "author": "Yan Kostadinov <yankostadinov@gmail.com>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
